AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
LOCALSTACK_ENDPOINT=http://localhost:4566
PORT=3000
ENTITIES_TABLE=entities
LISTINGS_TABLE=listings
TASKS_TABLE=tasks
AUDIT_LOG_TABLE=audit_log
ARTIFACTS_BUCKET=archieos-artifacts
 
# CORS origins (comma-separated). Include scheme and exact origin.
# Leave empty in local dev to allow all origins.
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080

# Rate limit
# Names must match src/plugins/simpleRateLimit.ts
RATE_LIMIT_MAX=5
RATE_LIMIT_WINDOW=10 seconds

# Intake pipeline (Local)
INTAKE_QUEUE_NAME=intake-queue
INTAKE_DLQ_NAME=intake-queue-dlq
INTAKE_QUEUE_URL=http://localhost:4566/000000000000/intake-queue
INTAKE_EVENTS_TABLE=intake_events

# LLM Classifier (feature flag)
USE_LLM_CLASSIFIER=false
LLM_CONFIDENCE_MIN=0.6
LLM_TIMEOUT_MS=6000
# Note: LLM_PROVIDER is defined in the "LLM Provider" section below.
# CloudFront (optional for signed GET)
USE_CLOUDFRONT=false
CF_DISTRIBUTION_DOMAIN=
CF_KEY_PAIR_ID=
# Base64-encoded PEM private key
CF_PRIVATE_KEY_B64=

# LLM Provider (OpenAI)
LLM_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
LLM_MAX_RETRIES=2
LLM_STREAM=false

# HTTP / CORS
ENABLE_CORS=true

# Auth (OIDC/Cognito) and Debug
ALLOW_DEBUG_USER=true
OIDC_ISSUER=
OIDC_AUDIENCE=
COGNITO_ISSUER=
COGNITO_AUDIENCE=

# Slack (verify + OAuth)
SLACK_BYPASS_VERIFY=true
SLACK_SIGNING_SECRET=
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=

# Docs (only enforced in production)
ENABLE_DOCS=true

# Task Catalog
TASK_DEFINITIONS_DIR=./config/task-definitions
