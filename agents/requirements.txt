# Core dependencies
python-dotenv==1.0.0
uvicorn==0.30.6
fastapi==0.115.0
pydantic==2.9.2
httpx==0.27.2

# OpenAI client (required by vendored OpenAI Agents SDK)
openai>=1.107.1,<2
# Vendored SDK runtime deps not installed via pip; include here explicitly
griffe>=1.5.6,<2
mcp>=1.11.0,<2
requests>=2.0,<3
types-requests>=2.0,<3

# AWS SDK (pin to compatible versions to avoid botocore conflicts)
# Align versions: aiobotocore 2.13.1 ↔ botocore 1.34.120..1.34.131 ↔ boto3 1.34.120..1.34.131
boto3==1.34.120
aiobotocore==2.13.1
aioboto3==13.1.1

# Matrix integration (for P3)
matrix-nio==0.25.2

# Monitoring
prometheus-client==0.21.0

# Development
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-mock==3.14.0

# The OpenAI Agents SDK is vendored at /external/openai-agents-python/
# We'll add it to PYTHONPATH instead of installing
